version: "3.9"

services:
  react-app:
    build:
      context: ./app
      dockerfile: Dockerfile

    image: aws-devops-react:latest

    container_name: react-app

    ports:
      - "8080:80"

    restart: unless-stopped